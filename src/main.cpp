#include <QApplication>

/*
#include "EditMode.h"
#include "MainWindow.h"
*/

int main(int argc, char** argv) {
    /*
    QApplication app(argc, argv);

    initEditModes();

    MainWindow master;
    master.show();

    return app.exec();
    */
}
